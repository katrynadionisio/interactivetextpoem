function setup() {
  createCanvas(windowWidth, windowHeight);
  textAlign(CENTER);
  textSize(20);
  colorMode(HSL);
}

function draw() {
  background('black');
  text ('And gave it up.', windowWidth/2, windowHeight/2);
  fill (255);
  
  if (mouseIsPressed) {
    textSize(60);
  
    }
  
  if (mouseIsPressed) {
    textSize (100);
  } else {
    textSize(20);

  }
  
  }
  